// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Supprimez cette partie si vous ne voulez pas d'export statique
  // images: {
  //   unoptimized: true,
  // },
}

module.exports = nextConfig
